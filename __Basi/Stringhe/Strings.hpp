#include "Strings.cpp"

int size(char*);
char* substringFromTo(char*, int, int);
char* substringFrom(char*, int);
char* substringTo(char*, int);
int strCmp(char*, char*);
char** split(char*, char, int &);